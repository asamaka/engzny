# Continuous Improvement Backlog

This file is automatically maintained by the Continuous Improvement Agent.
It provides continuity between agent runs — each agent reads this at the start
and updates it before pushing.

## Last Run

> **2026-03-05** | Trigger: `report_review` (report 11731094, 19349ms, breaking_news/breaking_news) | Fixed P1 regression: `imageUrl`/`photoUrl` stripping lost during `deb67a8` pipeline restructure. The enhance and review LLMs (Haiku, no web access) fabricate image URLs from training data. Original fix `d1a3d2c` added stripping to orchestrator callbacks, but the restructure rewrote all callbacks and dropped the stripping. Report 11731094 hero_summary had hallucinated `pic.twitter.com` URL. Fix: `stripFabricatedImageUrls()` helper applied to all 4 LLM callbacks (enhance onCardUpdate/onCardAdd, review onCardUpdate/onCardAdd) + initial hero card data from fast classifier. Research enrichment (`applyResearchToCards`) still adds legitimate Sonar-sourced URLs. UI rubric: report 11731094 — all 5 criteria PASS (7/7 populated, Daisy components, breaking_news 2-col layout, no clipping, 393x852 mobile viewport). No UI P0/P1 flagged.

## Active Work

Items currently being tracked across agent runs. Agents should pick up the
highest-priority incomplete item and continue where the last agent left off.

### P0 — Broken (errors, crashes, failed pipelines)

- [x] Reports missing screenshot thumbnail (hasThumb: false) — sharp fails silently on Vercel, no thumbnail saved (fixed: client-side thumbnail generation, sent with upload)
- [x] Reports missing render capture — html2canvas errors swallowed, no fallback (fixed: retry logic, DOM-only fallback, console warnings)
- [x] Pipeline reports "success" with 0 cards on 529 Overloaded — three compounding bugs: (1) Claude SDK maxRetries=2 insufficient, (2) skeleton cards not registered in currentCards on classify failure, (3) no zero-card guard. Fixed: maxRetries=4, register skeleton cards, detect zero-populated pipeline as error. Report b62c2bcd.
- [x] Enhance phase 404: invalid model `claude-sonnet-4-5-20250514` introduced by pipeline restructure `deb67a8` — model doesn't exist in Anthropic API, every enhance call fails silently (caught as non-fatal). Report 18e871e3 shows enhanceActions=0, all cards populated only by review fallback (10s delay). Fixed: changed to `claude-sonnet-4-20250514`.

### P1 — Degraded (slow, bad results)

- [x] LLM-hallucinated imageUrl/photoUrl — enhance phase generates fabricated Wikipedia/Unsplash URLs (e698e247 showed ant photo on emergency advisory, ea6f56b7 had unsplash, 89b5e4b3/19a4260b had Wikipedia). Fixed: strip imageUrl/photoUrl from enhance callbacks, removed prompt instructions, updated schemas. Research enrichment still adds real URLs.
- [x] imageUrl stripping regression — `deb67a8` pipeline restructure rewrote all orchestrator callbacks and dropped the `imageUrl`/`photoUrl` stripping from `d1a3d2c`. Report 11731094 hero had hallucinated `pic.twitter.com/t8a5P9yXfw`. Fixed: `stripFabricatedImageUrls()` helper applied to all 4 LLM callbacks + initial card data. Confirmed in reports 11731094, 47454a10, f48633b5.
- [x] Cards showing 2/N populated — partial card population not detected as failure (fixed: dashboard now counts unique cardIds from card+card_update+card_add events)
- [x] Sonnet enhancer only populates 1 out of N cards — tool_use loop missing, Claude stops after first tool call (fixed: added iterative tool_use loop in Claude adapter)
- [x] Pipeline 75-81s: review phase re-sends image unnecessarily, each tool loop iteration doubles input tokens (fixed: text-only review, doubled maxTokens, batch prompt hint)
- [x] Verification card status inconsistent: shows "unconfirmed" while 2/3 sources are green (fixed: post-phase reconciliation re-computes overall status from source statuses)
- [x] Verification card field name mismatches — LLM sends `status:"verified"` but reconciliation only checks `"confirmed"`, and LLM sends `.source` but code checks `.name` (fixed: normalizeSourceStatus + field fallbacks in orchestrator + frontend)
- [x] Haiku prematurely sets verification source statuses ("verified"/"confirmed") without research — Phase 2.5 skipped these sources entirely (fixed: prompt tells Haiku to leave as "checking", Phase 2.5 re-evaluates ALL sources against research regardless of current status)
- [x] Review phase 24.5s: Sonnet used for text-only structured merging (fixed: switched to Haiku + lean prompt + reduced maxTokens)
- [x] Enhance phase 18s with Sonnet — prompt sent all 22 schemas + 13 layouts (fixed: targeted schemas for relevant types only, compact layout info)
- [x] Enhance phase 27s with Sonnet — Sonnet too slow for tool_use card population (fixed: switched to Haiku, increased review maxTokens to 8192, compacted JSON)
- [x] Mobile uploads 15-17s + HTTP 413 errors — UPLOAD_TARGET_SIZE 3.2MB too close to Vercel 4.5MB limit (fixed: reduced to 1.5MB + MAX_DIM 1920→1568 to match Claude vision resolution)
- [x] Tool loop iterations blowing up pipeline duration — 7-card pipelines taking 31s+ due to 4-5 loop iterations per phase (fixed: maxIterations 2 for enhance, 1 for review, compact review prompt)
- [x] Enhance phase misses cards (6/7) when Claude returns end_turn early — review phase excluded unpopulated cards from prompt (fixed: review always includes unpopulated cards with NEEDS_POPULATION flag, orchestrator runs review even without research findings)
- [x] Verification card sources as raw URLs with "checking" hourglass — when LLM returns URL strings instead of source objects, frontend showed full URLs as names with misleading hourglass icon and no clickable link (fixed: extract hostname, set url, mark as "confirmed" — report 18e871e3)
- [x] Product pipeline 75.6s — enhance 33.2s + review 40.9s due to image re-processing and verbose research data (fixed: prompt caching via cache_control for tool loop, research data truncation in review/enhance prompts)
- [x] Enhance phase regression to Sonnet + web search — pipeline restructure `deb67a8` switched enhance from Haiku to Sonnet + web_search, causing 40-50s pipelines. Web search consumed tool iterations without calling update_card (0 actions). Fixed: reverted to Haiku (no web search, maxIterations=2). Reports 80de4b60/bb01443a/6e22a9e6 all 40-50s → expected 15-20s.
- [x] Review phase always runs LLM call when research findings exist even if all cards populated — 7s overhead for marginal enrichment (fixed: skip LLM review when all cards populated, programmatic research enrichment instead)
- [x] chart_card shows "undefined" and NaN% bars when LLM uses variant field names (e.g., `{team, minute}` instead of `{label, value}`) — fixed: normalize label/value from common LLM alternatives, fallback to first numeric field for value (report 4515184b)
- [x] stats_grid_card fragile to LLM field name variants — fixed: accept `name`/`title`/`metric`/`key` for label, `stat`/`number`/`amount` for value, `data`/`items` as array source alternatives
- [x] Sonnet model appears in 3/5 pipeline traces — was caused by hard-coded `source: 'sonnet'` label in orchestrator card events + `sonnetEnhancePromise` variable name. Actual model was always Haiku. Fixed: renamed to `source: 'enhance'` and `enhancePromise`. card-researcher.js fallback model string also noted (line 180) but doesn't affect functionality.
- [x] Verification sources show misleading `not_yet_reported` (clock icon) when research completed but verdict was `unverified`/`needs_context` — fixed: added `inconclusive` status with `help_outline` icon + warning color. Phase 2.5 now sets `inconclusive` when finding exists but verdict isn't binary. Frontend shows "Could not confirm or deny" subtitle. `not_yet_reported` icon changed from `schedule` to `remove_circle_outline` with "Not found in research" subtitle. Reports 80de4b60, db8c99a9, bb01443a all affected.
- [x] timeline_card field name mismatches — LLM sends `time`/`label`/`description` but renderer only checked `date` for time and `event`/`text`/`description` for text. The `time` field ("3 hours ago") was silently dropped and `label` ("Al Jazeera Report") was lost. Fixed: accept `time`/`timestamp`/`when`/`period` → date, `label`/`title`/`name` → event. Also renders label+description as structured text. Report ba1c7422.
- [x] Verification source matching too strict — Phase 2.5 used exact substring `text.includes(sourceName)` which failed for parenthetical source names like "Al Jazeera Mubasher (English)". Fixed: progressive matching — exact → strip parentheticals → keyword overlap (≥50% of 4+ char words). Report ba1c7422 had both sources as `not_yet_reported` despite research finding Al Jazeera data.
- [x] Verification sources as strings bypass Phase 2.5 entirely — LLM returns `["Al Jazeera"]` (string array) instead of `[{name, status}]` objects. Phase 2.5 tried `source.name` on a primitive string (undefined) and set properties on primitives (silently ignored). All research wiring was skipped. Frontend defaulted to `status: 'checking'` → "Checking..." with hourglass on completed pipelines. Fixed: `normalizeSourceArray()` converts strings to objects before processing. Report 9ca6d20b.
- [x] Verification summary truncated at 200 chars mid-word — user-facing summary cut off at "fast-moving c" losing critical context. Increased to 500 chars with word-boundary truncation. Report 9ca6d20b.
- [x] Non-verifiable content (entertainment, photo, messaging, etc.) waited 10-12s for Sonar research that returned 0 findings — pipeline capped at 20s for entertainment content. Fixed: skip deep research for non-research-worthy content types (saves 50%+ pipeline time). Also clear misleading "Unconfirmed" hero badge for non-news content. Report 67f2ec12.

### P2 — Polish (UX friction, visual quality, confusing output)

- [x] Card grid had cramped margins on mobile — padding was 8px, gap was 8px. Increased to 16px padding + 12px gap (mobile), 20px/14px (tablet), 16px gap (desktop). Follow-up section padding also fixed.
- [x] ALL DaisyUI/Tailwind padding broken — unlayered `* { padding: 0 }` reset overrides `@layer` styles (CSS cascade: unlayered beats layered regardless of specificity). Moved reset into `@layer base` so DaisyUI components and Tailwind utilities take proper effect. Root cause of zero-padding cards, cramped badges/pills, and all spacing issues.
- [x] Progress bar showed wrong card count (1-2 out of 7) and client telemetry reported completedCards=1-2 — `onCardUpdated` handler didn't count `card_update` events as completed cards. Fixed: now tracks newly populated cards, increments counter, updates progress text, triggers first-card-shown transition.
- [x] Progress bar showed "8/7" for 7-card pipeline and completedCards overcounted in telemetry — `onLayoutUpdate` rebuilt card grid without resetting `completedCards`/`populatedCardIds`, hero card counted twice (once from `card` event, once from layout rebuild). Also `onCardAdded` didn't add to `populatedCardIds`. Fixed: reset counters on layout rebuild, track card_add in populatedCardIds, cap progress at 95%. Report ba1c7422.
- [ ] Render capture shows full-length card grid — consider viewport-height clipping for very long card lists
- [x] Chat card field name mismatches — LLM sends `isOwn`/`timestamp` but renderer only checked `isUser`/`time`. Fixed: accept `isOwn`/`isOwnMessage`, `timestamp`/`ts`, `content`/`message` variants.
- [x] Follow-up questions always empty — fast classifier returns `[]`, research Sonar Q&A never surfaced. Fixed: orchestrator merges research follow-up questions, frontend re-renders on complete event.
- [ ] `getCardTypeLabel` function and `CARD_ICONS` for 6 new card types (chat_card, map_card, order_card, stats_grid_card, gallery_card, source_card) are dead code — never called. Consider removing or integrating.
- [x] Web research citations never displayed — `appendCitations()` queried `.card-inner` which no card renderer creates, silently dropping all Perplexity/Claude citations (fixed: append directly to card element, add horizontal margin to CSS)
- [x] Progress bar stuck at 95% for 10-15s after all cards populated — enhance finishes but pipeline waits for Sonar deep research. User sees "6/6" at 95% with rotating enhance messages. Fixed: `checkAllCardsReady()` shows 100% with green checkmark when completedCards >= totalCards. Research enrichment arrives silently.
- [x] Card renderer has no error boundary — if `renderCardContent` throws (e.g., `.map()` on non-array), the card fails silently and can break the layout forEach loop. Fixed: all 4 call sites wrapped in try-catch with `renderFallbackCard` fallback.
- [x] `did_you_know_card` renderer drops `context` field — LLM populates it (report 9e268ef6 had context on card-5) but it's not rendered. Other card types (person_card, key_metric, location_card) render context. Fixed: render context as styled info box below the fact, increased orchestrator truncation from 200→500 chars. Report d80370b1.
- [x] `person_card` research enrichment writes to `background` field which frontend never renders — research data invisible. Fixed: write to `notableInfo` instead (rendered with 💡 icon), increased limit 200→500 chars. Report 5bee7d01.
- [x] `location_card` research enrichment sets `details` as string but frontend expects array — causes `.map()` error caught by error boundary, falls back to generic renderer. Fixed: wrap in array. Report 5bee7d01.
- [x] `fact_check.explanation` and `news_card.relatedContext` research enrichment truncated at 150 chars — user-facing fields too short. Fixed: increased to 300 chars.
- [ ] Consider late research enrichment: when deep research times out at pipeline cap, research results are lost entirely. A future improvement could send research enrichment as late SSE card_update events after the `complete` event, so verification sources get wired even for slow Sonar calls. Requires frontend to handle card_updates after complete.
- [x] Verification card `lastChecked` showed raw ISO timestamp — unfriendly `2026-03-05T14:12:01.480Z` display. Fixed: `timeAgo()` utility shows "Just now", "2h ago", etc. with clock icon. Report 6df9b7de.
- [x] LLM text artifacts (trailing dots ".....", consecutive dots "..") in card summaries — `cleanSummary()` utility cleans up across verification_card, hero_summary, news_card, person_card, location_card, did_you_know_card, fact_check. Report 6df9b7de.
- [x] news_card field name normalization — accepts `title`/`heading` for headline, `publisher`/`outlet` for source, `publishedAt`/`time` for date, `description`/`content` for summary. Defensive against LLM field variants like other card types.
- [ ] Ongoing: review card rendering CSS on each run for visual regressions (margins, padding, spacing, typography, responsive breakpoints)
- [ ] Ongoing: agent diversification — if last 3+ commits are same area, agents must pick a different area (enforced via prompt + spec)
- [x] Remaining card types to audit for field normalization: action_card, comparison_card, order_card, product_card — all 4 now accept common LLM field name variants (same pattern as 6 previously fixed card types). All card types with object/array fields are now normalized.

### P3 — Resilience (logging, edge cases, retry logic)

- [x] Dashboard `eventsReceived` always showed 0 for all sessions — `logger.clientReport()` deleted `eventsReceived` array before Redis persistence without preserving the count. Fixed: store `eventCount` integer in Redis, dashboard reads `eventCount` as fallback.
- [ ] Adapter singleton cache uses module-level Map — could cause subtle issues across warm serverless invocations. Simplified cache key to `provider:model` to reduce stale entries.

## Observations

Patterns noticed across multiple runs that may inform future improvements.

- Initial backlog created 2026-03-02. System averaging 20-24s pipeline times.
- sendEvent was double-logging pipeline events (bare + explicit with meta). Fixed 2026-03-02.
- Pipeline durations trending down: recent reports at 16-21s range (vs earlier 20-24s).
- 100% of recent users are mobile (ua: "mobile"). Render captures will be iPhone-width by default.
- html2canvas added as CDN dependency (~40KB gzipped). Non-blocking, deferred load.
- Sonnet enhance+review phases each make one LLM call that returns stop_reason:tool_use — without looping, only first card per phase gets populated. Tool loop fix means ~4-8 iterations per phase but each card appears in real-time via SSE.
- Review phase was re-sending the base64 image in every tool loop iteration — for a 2333KB image, this adds massive input token cost. Switched to text-only for review since cards are already populated from the enhance pass.
- With maxTokens at 4096, Claude could only fit ~4 tool calls per response, forcing multiple round-trips. Increasing to 8192 lets Claude batch 7+ cards in one response.
- Phase 3 (review) overwrites Phase 2.5's verification status computation. The review LLM picks an overall status that may contradict the individual source icons the user sees. Added post-phase reconciliation to always re-derive status from source data.
- Report thumbnails depend on `sharp` which requires native binaries. On Vercel serverless, sharp fails silently. Moved thumbnail generation to client-side (canvas resize to 360px JPEG).
- html2canvas render capture had zero error visibility — all `.catch(() => {})`. Added console.warn, retry (up to 3 attempts for script loading), and DOM-only fallback when canvas capture fails.
- Review phase was using Sonnet (24.5s) for text-only card updates — a perfect fit for Haiku since no image analysis needed. Lean prompt with only relevant card schemas reduces input tokens significantly.
- Pipeline breakdown for 46s run: classify 1.5s + enhance 20s (parallel with research 11s) + review 24.5s. After Haiku review fix, expected: classify 1.5s + enhance 20s + review ~3-5s = ~25s total.
- Enhance prompt was sending all 22 card type schemas (~3500 tokens) + 13 layout type summaries (~500 tokens) even though classifier already chose specific types. For a "simple" layout with 4 cards, only 4 schemas are needed. Switched to targeted schemas: existing types + layout suggested types + did_you_know_card.
- Pipeline breakdown for 28.8s run (f843d2b8): classify 1.0s + enhance 18.1s (parallel with research 10.9s) + review 9.7s. After targeted schema fix, expected: classify 1.0s + enhance ~12s + review ~9s = ~22s total.
- Pipeline breakdown for 42.3s run (cc177943): classify 1.5s + enhance 27.3s (Sonnet, parallel with research 9.5s) + review 13.5s (Haiku, 4096 maxTokens). Switched enhance to Haiku and unified maxTokens to 8192. Expected: classify 1.5s + enhance ~6s + review ~6s = ~14-18s total.
- Haiku is sufficient for card population from screenshots — the task is structured extraction, not complex reasoning. Quality maintained by review phase as second pass.
- Client-side UPLOAD_TARGET_SIZE was 3.2MB (3.2 * 4/3 = 4.27MB base64 + JSON overhead → dangerously close to Vercel 4.5MB limit). Reduced to 1.5MB (~2MB base64) — eliminates HTTP 413 risk and halves mobile upload time. MAX_DIM reduced from 1920 to 1568 to match Claude's internal vision processing resolution.
- Dashboard showed 2x "Upload failed (HTTP 413)" client errors and upload times of 15-17s for ~2.6MB images on mobile. After fix: expected upload times ~5-8s, zero 413 errors.
- Tool loop iterations are the main pipeline duration driver for 7-card layouts. Each iteration re-sends the full conversation (including image in enhance phase), growing input tokens. Default maxIterations=8 allows up to 8 round-trips. Capped enhance at 2 (first pass populates most cards, second catches remainder) and review at 1 (all updates in a single batch). Also compacted review prompt to only include cards matching research findings — reduces input tokens by ~40% for typical 7-card pipelines.
- UI quality was a blind spot: card grid had 8px padding/gap on mobile, giving a cramped edge-to-edge look. No agent caught this because checks were focused on errors/performance. Agent spec updated to always review UI quality regardless of trigger reason.
- Pipeline 3da0bca5 had 6/7 cards because enhance phase (maxIterations=2) made only 1 LLM call that populated 6 cards with stop_reason: end_turn. The 7th card stayed as a loading skeleton. Root cause: buildReviewPrompt filtered cards to research-matching ones only, silently dropping empty cards. The review phase is text-only (no image) but can still populate cards like did_you_know_card, action_card, warning_card from content analysis + research context.
- ROOT CAUSE of zero-padding UI: the global `* { margin: 0; padding: 0; }` CSS reset was unlayered, which in CSS Cascade Layers means it overrides ALL @layer-based styles regardless of specificity. DaisyUI 5 + Tailwind 4 both use @layer for their styles. So every `.badge`, `.btn`, `.card`, `p-4`, `mb-2`, `gap-3` etc. had their padding/margin stripped. Fix: move reset into `@layer base`.
- Product pipeline d9c9b87f: 75.6s total, 145K input tokens. Enhance 33.2s + review 40.9s. The trace shows Sonnet model despite code specifying Haiku for both phases. Possible causes: adapter cache returning stale instance, API model routing, or warm serverless instance reuse. Added model logging (configModel vs adapterModel) to enhance function to help diagnose. Also added prompt caching (cache_control: ephemeral) to reduce re-processing cost on tool loop iteration 2, and truncated research data (summaries ≤200 chars, explanations ≤150 chars, max 8 findings) to cap review prompt size.
- Breaking news pipeline 6f376be3: 25.9s total (6520 input, 4108 output tokens). Enhance 17.5s (single Haiku call, 6 cards) + review 6.9s (text-only Haiku). Enhance duration is intrinsic to Haiku output generation speed (~2500 tokens for 6 tool calls). Review was marginal — all 6 cards were already populated, and Phase 2.5 already handled verification sources. Replaced LLM review with programmatic research enrichment (adds sourceUrls, context). Expected pipeline: classify 1.5s + enhance 17.5s + programmatic 0ms ≈ 19s.
- Client-side `completedCards` tracking was fundamentally broken: the enhance phase sends `card_update` events (via `onCardUpdate` callback), but the `onCardUpdated` handler in hub-v2.html never counted these as completed. Only `card` events (from `onCardPopulated`, typically just hero from Haiku phase) and `card_add` events were counted. Every session in Redis shows completedCards=1-2 despite 5-7 cards being rendered. This also meant the progress bar text ("X/Y") was stuck at low values during the enhance phase, and the `entering` CSS animation class wasn't applied to enhance-populated cards.
- The `fastClassify` function (used by orchestrator) always returns empty `topQuestions: []` and `followUpQuestions: []`. The full `layout-designer.js` asks for them in its prompt but is not used in the current pipeline. Sonar deep research is the only source of follow-up Q&A. The `complete` SSE event sends `contentAnalysis` but the frontend was ignoring it entirely — `onPipelineComplete()` never used the data parameter.
- Chat card schema defines `isUser` (boolean) and `time` (string) for messages, but the LLM frequently uses alternative names like `isOwn`, `isOwnMessage`, `timestamp`, `ts`, `content`, `message`. Making renderers defensive against common LLM field name variants is more reliable than expecting perfect schema adherence.
- Verification card had the same LLM field variant issue: schema says `name`/`confirmed` but Haiku sends `source`/`verified`. The `computeVerificationStatus` function only checked for exact `"confirmed"` match, causing all-green sources to compute as `"unconfirmed"`. This pattern will recur for any card type — **always normalize LLM-provided enum values before comparing**.
- Haiku should NEVER set verification statuses — it has no research data to back claims. Its role is speed-to-first-card with assumptions that get confirmed/denied by Sonar research. Phase 2.5 must always re-evaluate ALL sources against research, not just "checking" ones.
- The `appendCitations` function was referencing `.card-inner` — a class that was likely removed during a card renderer refactor but the citation code wasn't updated. This meant zero research citations were ever visible to users since the feature was added. Always verify DOM selectors against actual rendered HTML when making changes.
- UI Rubric (2026-03-03, report 89b5e4b3): Component consistency — inline `style=` attrs on cards still present in prod DOM (fix may not be deployed yet); Hub cohesion — PASS; Card integrity — 7/7 PASS; Population integrity — 7/7 PASS; Mobile readability — PASS (393px viewport, 16px padding). Overall: P1 at most (inline styles), pending deployment of recent UI commits.
- UI Rubric (2026-03-04, report ea6f56b7): All 5 criteria PASS. Daisy components, gap-4 p-4 grid, 6/6 populated, 393px mobile viewport, no clipping. Inline style issue from 89b5e4b3 is resolved — recent UI refactor commits are now deployed.
- UI Rubric (2026-03-04, report e698e247): All 5 criteria PASS. 9/9 cards populated, 393px viewport, Daisy components, consistent spacing, no clipping/overflow.
- LLM-hallucinated imageUrl is systemic: 3/4 reports had fabricated URLs (Wikipedia commons, Unsplash). The enhance LLM has no web access — any URL it generates comes from training data patterns, not actual web lookups. Even "plausible" URLs (like Wikipedia photo paths for real people) may point to wrong images, outdated photos, or non-existent resources. The ant image on e698e247 (Camponotus_flavomarginatus) proves the LLM doesn't understand URL semantics — it pattern-matched a Wikipedia commons path structure.
- Pipeline ea6f56b7: 22.1s total. Enhance 12.4s (Haiku, 6 cards), deep_research 20.6s (Sonar). Sonar is the bottleneck — enhance finishes at ~11s but pipeline waits until ~22s. Progress bar was stuck at 65% for 8+ seconds with stale "enhancing" phase messages. Fixed: heartbeat now detects enhance completion and switches to "researching" phase with advancing progress 70-90%.
- Programmatic research enrichment (`applyResearchToCards`) was missing imageUrl propagation, did_you_know_card context, person_card background, and location_card details. Only fact_check verdict and news_card relatedContext were enriched. Extended to cover 5 additional card types × fields.
- chart_card renderer assumed `{label, value}` schema compliance. LLM sent `{team: "Wolves (Gomes)", minute: 78}` for a sports goal timeline — same class of bug as chat_card and verification_card field mismatches. Pattern: **every card type with arrays of objects should normalize field names defensively** rather than trusting schema compliance.
- Report 00c854e0 (messaging/WhatsApp, 5 cards, 11.2s): research phase returned 2 follow-up Q&A with answers + 3 additional questions — all valuable context that was being discarded. Report f38c2ec1 (breaking news, 7 cards, 17.4s): person_card and location_card data was swapped by the LLM (person_card had location data and vice versa) — potential P2 output quality issue for future fix.
- Sonar deep_research latency is consistently 20-25s (reports: c5eb5600 at 23.8s, 9e268ef6 at 24.7s, ea6f56b7 at 20.6s). This is external API latency that can't be reduced. Pipeline duration cap (default 20s) ensures users aren't held hostage by Sonar. Trade-off: some pipelines may not get research enrichment (verification source wiring, context, URLs). With the cap, enhance-dominated pipelines (12-18s enhance) get 2-8s of research grace — enough for ~30% of Sonar calls to complete, not enough for the slowest ones. The cap is configurable via `PIPELINE_MAX_DURATION` env var.
- Pipeline restructure (`deb67a8`) introduced a 3-milestone flow with Sonnet + web search for enhance phase. The model name `claude-sonnet-4-5-20250514` was invalid (doesn't exist in Anthropic API). Because the enhance failure is caught as non-fatal, pipelines completed but ALL card population fell to the review phase (Haiku text-only), degrading UX. Report 18e871e3 confirms: enhanceActions=0, enhanceDuration=79ms (just the 404 error), reviewDuration=10076ms (populated all 6 cards). With the fix, enhance should populate cards progressively with web search validation.
- UI Rubric (2026-03-04, report 18e871e3): All 5 criteria PASS structurally. 6/6 cards populated via review fallback. Daisy components, consistent spacing, no clipping, mobile-width OK. Note: enhance phase was broken so no progressive SSE card updates — users saw skeletons for 15+ seconds until review completed.
- Verification card sources format inconsistency: LLM sometimes returns sources as URL strings (e.g., report 18e871e3) vs structured objects with name/status/snippet (e.g., reports db8c99a9, e698e247, 89b5e4b3). The review phase Haiku returns structured objects when it has research context, but URL strings when populating from raw research URLs. Frontend now handles both formats gracefully.
- Anthropic web_search_20250305 tool combined with custom tool_use is unreliable for card population: Sonnet uses web_search (server_tool_use → web_search_tool_result) but then generates text responses instead of calling update_card/add_card tools. Report 6e22a9e6 shows enhance phase with 2364 output tokens but 0 tool actions — all output was text, not tool calls. The web search apparently satisfies the model's information-seeking behavior, causing it to skip tool_use calls. Do NOT combine web_search with custom tools for structured data extraction. Use Sonar for web research instead (dedicated, reliable, parallel).
- Pipeline duration regression pattern: `deb67a8` introduced Sonnet + web search → `8fa84c8` fixed invalid model name but kept Sonnet → 3 pipelines at 40-50s before caught. Lesson: model changes in pipeline restructures need immediate pipeline timing verification. A P1 regression can hide behind a P0 fix (the 404 fix masked the performance regression).
- Verification source status mismatch root cause: The deep researcher prompt defines valid verdicts as `verified|misleading|unverified|false|partially_true|needs_context`, but Phase 2.5 only checked `verified|partially_true` → confirmed and `false|misleading` → denied. The `unverified` and `needs_context` verdicts (recommended for breaking news) fell through to `not_yet_reported` — affecting nearly every verification card since most breaking news research returns cautious verdicts. The `not_yet_reported` clock icon (`schedule`) looked identical to the loading state, misleading users into thinking research was still pending when it was actually complete.
- UI Rubric (2026-03-04, report 80de4b60): All 5 criteria PASS. 7/7 cards populated, Daisy components, gap-4/p-4 grid, no clipping, grid-cols-1 mobile layout. Note: verification sources had misleading `not_yet_reported` icons (now fixed with `inconclusive` status).
- Report b62c2bcd: 529 Overloaded on BOTH classify and enhance. SDK retried 2 times (3 total) but all failed. Sonar deep research ran but with skeleton data ("analyzing" contentType, no real card info) → returned useless "Unable to process request". Pipeline completed with 0 cards but `outcome: "success"`. Root cause chain: classify→null → skeleton used but NOT registered in currentCards → enhance received skeleton cards but onCardUpdate silently dropped updates (existing = undefined) → zero cards in final assembly → reported as success. Three-part fix: more retries, register skeletons, guard zero cards.
- The Anthropic SDK (v0.71.2) retries on status >= 500 (which includes 529), 408, 409, 429. Default maxRetries=2 (3 total attempts). Exponential backoff with jitter. For sustained overloads (>10s), 3 attempts may not be enough. Increased to maxRetries=4 (5 total attempts with longer total wait window).
- UI Rubric (2026-03-04, report ba1c7422): All 5 criteria PASS. 7/7 cards populated, Daisy components, gap-4/p-4 grid, no clipping, grid-cols-1 mobile on 393x852. Verification sources show `not_yet_reported` (fix pending deployment from this branch).
- Client session ba1c7422 reported `completedCards: 8` for 7-card pipeline. Root cause: hero_summary counted once by `onCardReceived` (from `card` SSE event during Haiku phase) and again by `onLayoutUpdate` (which rebuilds grid with `populatedData`). `onLayoutUpdate` never reset `completedCards` or `populatedCardIds` when rebuilding. This caused the navbar progress text to briefly show "8/7" and the telemetry to permanently record overcounted cards.
- Dashboard sessions endpoint always showed `eventsReceived: 0` because `logger.clientReport()` stripped the array before Redis persistence. The dashboard `formatSessions` tried `r.eventsReceived?.length` which is always undefined for Redis-loaded sessions. Fixed by storing integer `eventCount` alongside the session.
- Three 0-card pipelines (1981dd87, bcbb42f5, fb7698a8) occurred during the same API overload window as b62c2bcd. All had 222KB images, `contentType: "analyzing"`, `outcome: "success"`, and SSE stall detections. These are covered by the 0-card guard fix in fbcc525.
- timeline_card is the 5th card type to need LLM field name normalization (after chat_card, verification_card, chart_card, stats_grid_card). **Pattern: every card type with object fields should accept common LLM variants.** Remaining card types to audit: action_card (label/name), product_card (features/warnings), comparison_card (columns/rows), order_card (items). Lower priority since these are less commonly generated.
- Verification source matching was too rigid — `text.includes(fullSourceName)` only works when the research text uses the exact same phrasing as the source name. For qualified names like "Al Jazeera Mubasher (English)" or "Israeli Media Reports", this almost never matches. Progressive fuzzy matching (strip parentheticals → keyword overlap) is more robust without significant false-positive risk since sources are already thematically related to the claim.
- UI Rubric (2026-03-04, report ba1c7422, re-run): All 5 criteria PASS. 7/7 populated, Daisy components, gap-4/p-4 grid, no clipping, 393x852 mobile. Timeline card had empty dates + missing labels (now fixed). Verification sources showed `not_yet_reported` despite research data (fuzzy matching fix addresses this).
- Verification card sources have a third failure mode: LLM returns string array `["Al Jazeera"]` instead of object array `[{name: "Al Jazeera", status: "checking"}]`. This is the 6th card type field variant issue (after chat_card, verification_card objects, chart_card, stats_grid_card, timeline_card). Pattern: **any card field that expects an array of objects should also handle an array of strings**. The frontend already handled this case defensively (line 1957-1964 in hub-v2.html), but the backend Phase 2.5 did not.
- Verification summary was user-facing text truncated at 200 chars — same limit used for LLM-to-LLM context fields (relatedContext, background, details). User-facing fields should have higher limits. The 200-char limit was appropriate for reducing LLM input tokens in enhance/review prompts, but the verification summary goes directly to the browser.
- UI Rubric (2026-03-04, report 9ca6d20b): All 5 structural criteria PASS (7/7 populated, Daisy components, gap-4/p-4 grid, no clipping, mobile viewport). Verification source "Al Jazeera" displayed as "Checking..." with hourglass — P1 output quality issue (now fixed by normalizeSourceArray).
- UI Rubric (2026-03-04, report 9e268ef6): All 5 criteria PASS. 6/6 cards populated, social_feed layout, Daisy components, gap-4/p-4 grid, no clipping, mobile viewport. No UI P0/P1 flagged.
- Pipeline 9e268ef6 (news, 26.3s): classify 1.5s + enhance 12.9s + deep_research 24.7s (Sonar). Enhance populated all 6 cards by ~14s, but pipeline waited 12+ more seconds for Sonar. Progress bar was stuck at 95% showing "6/6" — user has all content but UI signals "still working." This is the same Sonar latency pattern as ea6f56b7 (20.6s research). Sonar API speed is external — only fix is better perceived performance via early visual completion.
- Card renderer error boundary: `renderCardContent` is a 500-line switch statement covering 20+ card types. A single bug (e.g., `.map()` on a non-array from LLM field variants) throws an uncaught exception that propagates up to the SSE event handler. In `onLayoutUpdate`, this happens inside a `forEach` — one card error skips all remaining cards. The try-catch + fallback pattern is essential insurance against the never-ending stream of LLM field variant surprises.
- Research enrichment (`applyResearchToCards`) had a systemic pattern of writing to fields the frontend doesn't render or expecting wrong types. `person_card.background` was never rendered (frontend uses `notableInfo`). `location_card.details` was set as string but frontend expects array. These are invisible data losses — pipelines look successful but research context is silently dropped. Pattern: **always verify enrichment target fields exist in the frontend renderer**.
- Truncation limits in `applyResearchToCards` were inconsistent: `did_you_know_card.context` 500, `verification_card.summary` 500, but `fact_check.explanation` 150, `news_card.relatedContext` 150, `person_card.background` 200, `location_card.details` 200. User-facing fields should all use 300-500 chars. LLM-to-LLM fields (used as prompt context) can stay at 150-200.
- UI Rubric (2026-03-04, report 5bee7d01): All 5 criteria PASS. 7/7 cards populated, Daisy components, breaking_news 2-col layout, gap-4/p-4 grid, no clipping, mobile viewport (393px). person_card had invisible `background` field (now fixed → `notableInfo`). No UI P0/P1.
- UI Rubric (2026-03-05, report 6df9b7de): All 5 criteria PASS. 7/7 cards populated (hero_summary, verification_card, person_card, location_card, timeline_card, news_card, did_you_know_card), Daisy components throughout, breaking_news 2-col layout, gap-4/p-4 grid, no clipping/overflow, mobile viewport. P2 display quality: verification_card `lastChecked` showed raw ISO timestamp, LLM summary had trailing dots ("....." artifacts). Fixed in this run.
- news_card is the 6th card type to receive field name normalization (after chat_card, verification_card, chart_card, stats_grid_card, timeline_card). news_card is commonly generated in breaking_news pipelines. LLM field variant pattern: `title` vs `headline`, `publisher` vs `source`, `publishedAt` vs `date`, `description`/`content` vs `summary`.
- comparison_card, action_card, order_card, product_card are the 7th-10th card types to receive field name normalization (2026-03-05). **All card types with object/array fields now have defensive normalization.** This completes the systematic field normalization effort across all 10 affected card types. Pattern: every card type that contains arrays of objects or structured fields needs to accept common LLM-generated alternatives for every property name.
- LLM text artifact pattern: Sonar research API and Claude both produce summaries with consecutive/trailing dots ("...", ".....", ".. "). These are concatenation or formatting artifacts from multi-source synthesis. The `cleanSummary()` utility normalizes: `.{2,}` → `.`, trims trailing dots, removes excessive period-space sequences. Applied to all user-facing card text fields (summaries, contexts, explanations, claims, snippets, facts). Not applied to structural fields (titles, names, URLs) or `text_extract` card (user's original text should be preserved).
- Pipeline 67f2ec12 (entertainment/satire, 5 cards): classify 1.4s + enhance 9s + research 20s (cap). Research returned 0 findings — Sonar web search is unproductive for non-factual content (memes, personal photos, chat, settings). With research skip, expected pipeline: classify 1.4s + enhance 9s ≈ 10s (50% reduction). Content types worth researching: breaking_news, news, emergency, sports, science, health, finance, tech, social_media. These are fact-checkable, time-sensitive, or require external verification.
- Hero card `investigationStatus` was always overwritten to "unconfirmed" at pipeline end when no verification card or research findings exist. For entertainment content, this produces a misleading gray "Unconfirmed" badge on comedy posts. Fix: only set investigationStatus when research was attempted. Frontend hides the status pill when null.
- Sonar research API returns citation markers `[1][2][3]` in follow-up Q&A answers. These reference Sonar's internal source list which is not passed through to the frontend. In report 47454a10, all 3 follow-up answers had markers (e.g., "killing of Iran's Khamenei[2][3][7]"). Added `cleanCitations()` to strip `\[\d+\]` patterns. Report f48633b5 had no markers — depends on Sonar response format.
- `overallContext` field from Sonar deep research provides a concise 1-2 sentence situational summary (e.g., "Fast-escalating Israel-Hezbollah clashes in early March 2026..."). This was being stored in report data but never rendered. Now displayed as a styled context card above the Q&A list. Available in all recent breaking_news reports.
- Follow-up `additionalQuestions` (from Sonar) are question strings without answers. Previously rendered as expandable accordions with "Expand for quick context…" placeholder — misleading since expanding showed nothing useful. Now rendered as simple non-expandable list items with a help icon, clearly indicating these are suggested questions rather than answered ones.
- Pipeline restructures are a regression vector: `deb67a8` rewrote all orchestrator callbacks and silently dropped the `imageUrl`/`photoUrl` stripping from `d1a3d2c`. The fix was present in the prompt ("DO NOT generate imageUrl") but not enforced in code. Lesson: when rewriting callback code, grep for all safety strips/guards in the original and ensure they're preserved. The `stripFabricatedImageUrls()` helper centralizes this so future restructures only need one function call.
- UI Rubric (2026-03-05, report 11731094): All 5 criteria PASS. 7/7 cards populated (hero_summary, verification_card, person_card, location_card, timeline_card, news_card, did_you_know_card), Daisy components, breaking_news 2-col layout, gap-4/p-4 grid, no clipping/overflow, 393x852 mobile viewport. P1 output quality: hero `imageUrl` was hallucinated `pic.twitter.com/t8a5P9yXfw` (fixed by restoring image stripping).
